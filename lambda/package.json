{
  "name": "notion-to-text-lambda",
  "description": "Lambda function to get content of publicly shared notion .site pages",
  "version": "0.1.0",
  "private": false,
  "author": "pirafrank",
  "url": "https://fpira.com/",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/pirafrank/notion-to-text.git"
  },
  "scripts": {
    "clean": "rm -rf node_modules",
    "pack": "rm -f function.zip; zip -r function.zip . ; ls -lah function.zip"
  },
  "dependencies": {
    "chrome-aws-lambda": "^10.1.0",
    "puppeteer-core": "^10.0.1",
    "notion-to-text-core": "../core"
  },
  "devDependencies": {
    "puppeteer": "^12.0.1"
  }
}
