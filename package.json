{
  "name": "notion-to-txt",
  "description": "render publicly shared notion web pages as raw text",
  "version": "0.1.0",
  "private": true,
  "author": "pirafrank",
  "scripts": {
    "cli": "node ./src/cli",
    "server": "node ./src/server",
    "debug": "DEBUG_ENABLED=true node ./src/server",
    "pack": "rm -f function.zip; zip -r function.zip ."
  },
  "dependencies": {
    "axios": "^0.21.1",
    "chrome-aws-lambda": "^10.1.0",
    "cross-env": "^7.0.3",
    "dotenv": "^8.6.0",
    "fastify": "^3.24.0",
    "he": "^1.2.0",
    "html-to-text": "^8.1.0",
    "puppeteer-core": "^10.0.1"
  },
  "devDependencies": {
    "puppeteer": "^12.0.1"
  }
}
